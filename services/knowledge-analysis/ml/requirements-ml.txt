# Training/notebook deps ONLY — NOT installed into the service container.
# The service serves the trained model with numpy alone (already in requirements.txt).
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
seaborn>=0.13
scikit-learn>=1.3      # metrics/curves + PCA only (NOT the model)
datasets>=2.16         # optional: Hugging Face fazni/roles-based-on-skills cross-check
kaggle>=1.6            # optional: Stack Overflow survey download
nbformat>=5.9          # build the notebook
nbconvert>=7.0         # execute/export the notebook
ipykernel>=6.0
